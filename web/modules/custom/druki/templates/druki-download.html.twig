{% set bem_block = 'druki-download' %}
<div{{ attributes.addClass(bem_block) }}>
  <h1 class="{{ bem_block }}__title">Скачать Drupal</h1>
  <h2 class="{{ bem_block }}__subtitle">Всего одна команда отделяет вас от Drupal 🚀</h2>

  <div class="{{ bem_block }}__command-pane">
    <span class="{{ bem_block }}__command">
      composer create-project drupal-composer/drupal-project:8.x-dev <strong>my_new_site</strong> --no-interaction
    </span>
  </div>

  <div class="{{ bem_block }}__command-note">
    <p>Узнайте больше о <a href="#">Composer</a> и <a href="#">Composer Drupal Project</a>. Возникли сложности? Обратитесь к <a href="#">руководству по установке</a>.</p>
  </div>

  <div class="{{ bem_block }}__cards">
    <div class="{{ bem_block }}__card">
      <div class="{{ bem_block }}__card-icon {{ bem_block }}__card-icon--try"></div>
      <h3 class="{{ bem_block }}__card-title">
        Демо-сайт на Drupal
      </h3>
      <div class="{{ bem_block }}__card-text">
        <p>Попробуй Drupal совершенно бесплатно, онлайн.</p>
      </div>
      <div class="{{ bem_block }}__card-bottom">
        <a href="http://ply.st/drupal/8.6.14" class="{{ bem_block }}__card-link">Попробовать Drupal</a>
      </div>
    </div>

    <div class="{{ bem_block }}__card">
      ss
    </div>

    <div class="{{ bem_block }}__card">
      ss
    </div>

    <div class="{{ bem_block }}__card">
      ss
    </div>

    <div class="{{ bem_block }}__card">
      ss
    </div>

    <div class="{{ bem_block }}__card">
      ss
    </div>
  </div>
</div>
