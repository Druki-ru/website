{% extends 'region.html.twig' %}

{% block region %}
  <header{{ attributes.addClass(classes) }}>
    <div class="layout-container {{ bem('container', [], block) }}">
      {{ block('content') }}
    </div>
  </header>
{% endblock %}
