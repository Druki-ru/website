{% set bem_block = 'app' %}
<div class="{{ bem_block }}">
  {% include '@autumn/include/header.html.twig' %}
  {{ page.title }}
  <main{{ main_layout_attributes.addClass(bem_block ~ '__content') }}>
    {{ page.sidebar_first }}
    {{ page.content }}
    {{ page.sidebar_second }}
  </main>
</div>
